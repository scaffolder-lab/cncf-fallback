apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cncf-fallback
  namespace: argocd
  labels:
    backstage.io/kubernetes-id: cncf-fallback
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/scaffolder-lab/cncf-fallback.git
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
